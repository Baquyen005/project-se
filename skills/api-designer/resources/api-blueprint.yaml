openapi: 3.0.0
info:
  title: API Documentation
  version: 1.0.0
paths:
  /example:
    get:
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardResponse'